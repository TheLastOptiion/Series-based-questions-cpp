/////////////////////////////////////////--------------Recursion Call

double e(double x,double n) {
    static double s=1;
    if(n==0)
        return s;
    s=1+x/n*s;
    return e(x,n-1);
}


cout<<e(3,10);

use setprecision(10) for more accuracy..
